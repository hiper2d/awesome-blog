spring:
  application:
    name: service-discovery

  cloud:
    config:
      uri: http://config
      fail-fast: true
      password: 123
      username: user

---
spring:
  profiles: local
  cloud:
    config:
      uri: http://localhost:9000
      fail-fast: true
      password: 123
      username: user